The RealTimeZones planner UI is considered production-ready.

Do NOT redesign the layout.

Do NOT move existing components.

Do NOT change spacing, colors or visual hierarchy unless fixing a bug.

Treat the planner as Version 1.0.

Only implement the requested feature while preserving the existing design system.